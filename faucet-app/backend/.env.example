# Backend Configuration

# Server Port
PORT=3001

# Blockchain Configuration
RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
CONTRACT_ADDRESS=0x3e2117c19a921507ead57494bbf29032f33c7412

# Wallet Configuration (NEVER SHARE THIS!)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend Configuration
FRONTEND_URL=http://localhost:5173

# Environment
NODE_ENV=development

# ⚠️ IMPORTANT SECURITY NOTES:
# 1. NEVER commit the real .env file to Git
# 2. NEVER share your PRIVATE_KEY
# 3. The wallet with this PRIVATE_KEY needs Sepolia ETH for gas
# 4. Change JWT_SECRET to a random string in production
# 5. Use environment variables on your hosting platform

# How to get values:
# - RPC_URL: Create free account at https://infura.io or https://alchemy.com
# - PRIVATE_KEY: Export from MetaMask (Account Details > Export Private Key)
#   ⚠️ WARNING: Only use a test wallet with minimal funds!
# - JWT_SECRET: Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
